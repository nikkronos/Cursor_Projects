# Архив неактуальных и завершённых проектов

Здесь лежат **снимки** проектов, которые не ведутся как основной рабочий код в этом монорепо. Активный отдельный репозиторий **VPN** — в `Projects/VPN/` (не в этой папке).

Пути в `Main_docs/PROJECTS.md` и `Main_docs/RULES_CURSOR.md` обновлены на `Projects/Non actual/…`.

**Примечание:** у каталогов `Damir/`, `xxx/`, `Transcribator/` вложенные `.git` удалены, чтобы файлы архива коммитились в `Cursor_Projects`; отдельная история этих репозиториев — на GitHub (`Futures_auction`, `kopiya-iksuyemsya` и т.д.).
