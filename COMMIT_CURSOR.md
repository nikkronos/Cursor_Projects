# Правила коммитов для Cursor

## Логика коммитов

### Что коммитится в репозиторий:

✅ **Коммитятся:**
- Все файлы из корня `CURSOR_TEST`
- Проект `TradeTherapyBot/` (полностью)
- Документация в корне (`docs/`, `RULES_CURSOR.md`, `QUICK_START_AGENT.md` и т.д.)
- Все общие файлы и настройки

❌ **НЕ коммитятся:**
- `ParentChildResearch/` - временный исследовательский проект
- `PastuhiBot/` - локальная копия боевого бота (хранится только локально)

### Настройка .gitignore

В корне проекта должен быть `.gitignore` со следующими исключениями:

```
# Временные проекты - не коммитить
ParentChildResearch/
PastuhiBot/
```

## Процесс коммита

### Автоматический коммит (предпочтительно)

Агент может делать коммиты автоматически после:
1. ✅ Все изменения протестированы
2. ✅ Проверены связанные функции
3. ✅ Нет ошибок линтера
4. ✅ Документация обновлена
5. ✅ Временные файлы удалены

**Команды, которые выполняет агент:**
```bash
# 1. Проверить статус
git status

# 2. Добавить все изменения (ParentChildResearch и PastuhiBot автоматически исключатся)
git add .

# 3. Сделать коммит с описанием
git commit -m "тип: краткое описание

- Детали изменений
- Что было исправлено
- Что было добавлено"

# 4. Отправить в репозиторий
git push origin main
```

### Ручной коммит (если нужно)

Если агент не может выполнить команды автоматически (проблемы с кодировкой, доступом и т.д.), он предоставляет пользователю команды для выполнения вручную.

## Типы коммитов

Используй стандартные типы коммитов:

- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление/изменение тестов
- `chore:` - обновление зависимостей, настройка и т.д.

**Примеры:**
```
feat: добавлена система массовых уведомлений
fix: исправлены SQL-инъекции в add_channel.py
docs: унифицирована структура документов
refactor: улучшена структура базы данных
```

## Важные правила

1. **Всегда проверяй .gitignore** - убедись, что ParentChildResearch и PastuhiBot исключены
2. **Проверяй статус перед коммитом** - `git status` покажет, что будет добавлено
3. **Пиши понятные сообщения коммитов** - описание должно быть ясным
4. **Коммить только после тестирования** - не коммить сломанный код
5. **Обновляй документацию** - если изменилась структура, обнови соответствующие файлы

## Структура репозитория

```
CURSOR_TEST/                    # Корень репозитория
├── .gitignore                  # Исключает ParentChildResearch и PastuhiBot
├── RULES_CURSOR.md            # Правила работы с проектами
├── QUICK_START_AGENT.md       # Быстрый старт для агента
├── COMMIT_CURSOR.md           # Этот файл
├── docs/                       # Общая документация
│   └── AGENT_PROMPTS.md       # Полная инструкция для агента
├── TradeTherapyBot/            # ✅ Коммитится
├── ParentChildResearch/        # ❌ НЕ коммитится
└── PastuhiBot/                 # ❌ НЕ коммитится
```

## CI/CD

Если настроен CI/CD (GitHub Actions):
- После `git push` автоматически запускаются тесты
- При успешных тестах → автоматический деплой на сервер
- Проверяй статус в GitHub Actions

## Частые проблемы

### Проблема: PowerShell не может выполнить команды из-за русских символов в пути

**Решение:** Агент предоставляет команды для ручного выполнения или использует альтернативные методы.

### Проблема: Файлы из ParentChildResearch или PastuhiBot попали в коммит

**Решение:** 
1. Проверь `.gitignore` - должны быть исключения
2. Если файлы уже добавлены: `git reset HEAD <файл>` и добавь в `.gitignore`
3. Если файлы уже закоммичены: `git rm --cached -r <папка>` и обнови `.gitignore`

---

**Последнее обновление:** 23.12.2025

